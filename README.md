# Learning Python 🐍

Python fundamentals from an experienced C# developer's perspective.
Each file is runnable: `python 01-fundamentals/01_variables_types.py`

## Progress

- [x] `01_variables_types.py` — Dynamic typing, str/int/float/bool/None, `type()`
- [x] `02_strings.py` — f-strings, `.strip()/.split()/.join()`, slicing `[:5]`, `[::-1]`
- [x] `03_lists.py` — List CRUD, slicing, sort, list comprehensions (vs `Select`/`Where`)
- [x] `04_dicts.py` — Dict CRUD, `.get()`, `.items()`, `in` operator (vs `Dictionary`)
- [x] `05_control_flow.py` — `if/elif/else`, `for/in`, `range()`, `enumerate`, `while`
- [x] `06_functions.py` — `def`, default args, multiple returns, `*args`/`**kwargs`, lambdas
- [x] `07_operators.py` — Arithmetic (`**`, `//`), comparison, chained, logical (`and`/`or`/`not`), identity (`is`), membership (`in`)

_Check/uncheck boxes in VS Code preview or on GitHub to track your progress._
